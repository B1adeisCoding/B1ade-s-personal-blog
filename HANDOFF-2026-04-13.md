# Personal Blog Handoff

## 当前状态

项目目录：

- `/Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog`

当前框架：

- Astro 6
- 静态站点
- Markdown 内容源
- `article` / `note` 双内容模型

当前总体进度：

- 第一版工程骨架：已完成
- 真实内容导入：已完成
- 公开发现路径与半隐藏规则：已完成基础版
- 正文 Obsidian 双链转站内链接：已完成
- 基础测试与构建验证：已完成
- 视觉精修 / SEO 完整化 / 部署：未完成

## 已完成工作

### 1. 工程初始化

- 创建独立博客工程 `personal-blog`
- 建立 Astro 基础配置
- 建立统一布局、导航、页脚和基础样式

关键文件：

- `/Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog/astro.config.mjs`
- `/Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog/src/layouts/Layout.astro`
- `/Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog/src/components/Header.astro`

### 2. 内容模型与查询层

- 建立 `articles` / `notes` 两个 collections
- frontmatter schema 已收口为：
  - `title`
  - `type`
  - `category`
  - `tags`
  - `date`
  - `updated`
  - `hidden`
  - `summary`
- 建立统一内容查询层，支持：
  - 全量内容读取
  - 公开 / 隐藏内容过滤
  - 分类聚合
  - 标签聚合
  - 最近更新
  - 搜索索引生成

关键文件：

- `/Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog/src/content.config.ts`
- `/Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog/src/lib/content.ts`
- `/Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog/src/lib/content-model.ts`

### 3. 页面实现

已完成页面：

- 首页 `/`
- 分类页 `/categories/`
- 分类详情页 `/categories/[category]/`
- 标签页 `/tags/`
- 标签详情页 `/tags/[tag]/`
- 搜索页 `/search/`
- 搜索索引 `/search-index.json`
- 关于页 `/about/`
- 隐藏分类页 `/hidden/[category]/`
- 文章详情 `/articles/[slug]/`
- 笔记详情 `/notes/[slug]/`
- 404 页

### 4. 半隐藏内容规则

当前规则：

- `hidden: true` 内容不进入首页公开发现路径
- 不进入公开主导航
- 不进入标签聚合
- 不进入公开搜索索引
- 允许通过 `/hidden/...` 和内容直链访问

注意：

- 这是“降低可见性”，不是权限控制
- 不具备安全隔离能力

### 5. 真实内容导入

已从以下目录导入：

- `/Users/b1ade/cursorProjects/GeneralUses/简历/八股合集`
- `/Users/b1ade/cursorProjects/GeneralUses/简历/AI相关`

导入规则：

- `八股合集` 默认导成 `note`
- `AI相关` 默认导成 `article`
- 文件名含 `初稿` 的内容导成 `note`，并标记 `hidden: true`
- 自动生成 frontmatter
- 自动推断 category / tags / summary

关键脚本：

- `/Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog/scripts/import-source-content.mjs`

重复导入命令：

```bash
cd /Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog
npm run import-content
```

### 6. 正文双链处理

导入器现在会处理 Obsidian 风格双链：

- `[[目标]]` -> 站内 Markdown 链接
- `[[目标|别名]]` -> 站内 Markdown 链接
- 找不到映射目标时保留成普通文本
- `![[图片]]` 当前降级为提示文本，不做图片导入

### 7. 文档与验收说明

已补：

- 项目 README
- 本地运行方式
- 基础验收路径

关键文件：

- `/Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog/README.md`

## 当前验证结果

最近一次已验证通过：

- `npm run import-content`
- `npm run build`
- `npm test`

构建状态：

- 当前可成功生成 69 个静态页面

## 当前已知问题 / 非阻塞缺口

### 1. 导入摘要质量一般

- `summary` 目前是启发式抽取
- 对部分技术长文不够自然
- 不影响构建，但影响列表页观感

### 2. 标签比较粗

- 标签来自“类别行 + 文件名推断”
- 当前可用，但不精细
- 后续可以做人工校正或单独映射表

### 3. 分类命名还偏原始

- 如 `AI Agent`、`Agent Skills`、`面试八股`、`消息队列`
- 已能用，但还没做统一的内容策展

### 4. 图片资源未接入

- 原文里的 `![[Pasted image ...]]` 没有导入
- 当前被降级为普通提示文本

### 5. SEO 只做了基础层

- 还没有 sitemap
- 还没有 RSS
- 还没有 OG 图
- 站点域名还是占位值 `https://example.com`

### 6. README 已更新，但没有完整“内容维护手册”

- 后续可补一份 `CONTENT-WORKFLOW.md`

## 明确未做的工作

这些目前都没做，下一窗口不要误判为已完成：

- 部署到真实托管平台
- 绑定真实域名
- sitemap
- RSS
- OG image 生成
- 图片资源导入
- 内容目录页内的自动相关文章推荐
- 分类与标签的人工策展
- 更强的全文搜索能力
- 正文目录 / heading anchor / 返回顶部等阅读增强

## 下一步建议

推荐优先级从高到低：

1. 把站点域名和部署方式定下来
2. 补 sitemap / RSS / OG
3. 优化导入规则，提升 summary / category / tags 质量
4. 处理图片资源导入
5. 做阅读增强而不是继续堆页面

## 如果开新窗口，建议直接给的新指令

可以直接贴这句：

```text
继续 `/Users/b1ade/cursorProjects/GeneralUses/简历/personal-blog` 这个 Astro 博客项目。先阅读 `HANDOFF-2026-04-13.md`，按里面的当前状态继续，不要重做已完成的内容。
```

## 补充说明

- 这次工作区根目录不是 git 仓库，所以没有做统一 commit
- `personal-blog` 自己带了独立 `.git`，但本轮没有主动提交 commit
- 运行命令时偶尔会看到 `jenv` 的权限提示：
  - `/Users/b1ade/.jenv/jenv.version: Operation not permitted`
  - 这是环境噪音，不影响当前项目构建和测试
